services:
    backend:
        build:
            context: ./backend
        ports:
            - "8000:8000"
        env_file:
            - ./backend/.env

    frontend:
        build:
            context: ./frontend
        ports:
            - "3000:3000"
        environment:
            # Frontend proxies /api requests to backend internally
            - NODE_ENV=production
